
<% if(alert.title !="" ) { %>
<script>
    Swal.fire({
        title: '<%= alert.title %>',
        text: '<%= alert.text %>',
        icon: '<%= alert.icon %>',
        confirmButtonText: 'OK'
    });
</script>
<% } %>